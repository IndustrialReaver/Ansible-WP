---
- name: Install WordPress, MariaDB, Firewalld, Nginx, and PHP-FPM
  hosts: WP-Candidate-65359de9
  remote_user: root
  # remote_user: user
  # sudo: yes

  roles:
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
