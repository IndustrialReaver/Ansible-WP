---
- name: Install WordPress, MariaDB, Firewalld, Nginx, and PHP-FPM
  hosts: ec2.py
  remote_user: root
  # remote_user: user
  # sudo: yes

  roles:
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
